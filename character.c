#include<stdio.h>
#include<conio.h>
int main()
{
char a[50];
int count=0,i;
printf("enter the word");
scanf("%s",&a[i]);

while(a[i]!=NULL)
{
count++;
i++;
}
printf("%d",count);
return 0;
getch();
}
